const initialState = {
  cardsData: [],
}

const CardManagmentReducer = (state = initialState, action) => {
  switch (action.type) {
    case
    default: {
      return { ...initialState }
    }
  }
}
